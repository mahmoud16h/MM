export const incrementPictureIndex = (index, pictureSize) => {
    if(index =)
    return {
        type: 'INCREMENT'
    }
}

export const decrementPictureIndex = () => {
    return {
        type: 'DECREMENT'
    }
}